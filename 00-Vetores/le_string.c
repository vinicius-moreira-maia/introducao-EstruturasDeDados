#include <stdio.h>

void main()
{
    // string é vetor de caracteres, e o nome de um vetor é ponteiro para o primeiro elemento

    char nome[50];
    scanf("%s", nome);

    printf("\nnome: %s\n", nome);
}